DATABASE_URL=postgres://kenr@localhost:5433/naval_tracker_dev

# Tick interval in milliseconds. Defaults to 30000 (per spec §9).
# POLL_INTERVAL_MS=30000

# To use the real aisstream.io feed instead of the mock source, set both:
#   AISSTREAM_ENABLED=true
#   AISSTREAM_API_KEY=<key from https://aisstream.io>
AISSTREAM_ENABLED=false
AISSTREAM_API_KEY=
